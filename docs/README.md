# Project Docs Index

The original design document package is preserved in `../裂隙守夜人_docs/` and was not moved during Phase 0 initialization.

Phase 0 created this `docs/` directory so future Godot-project-local notes can live under the expected `res://docs/` path without duplicating or overwriting the source document package.